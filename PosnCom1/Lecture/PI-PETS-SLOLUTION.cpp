#include <bits/stdc++.h>

using namespace std;

int main()
{
    set<int> a;
    set<int> a;
    set<int> a;
    for(int i = 0; i < n; i++){
        int temp;
        cin >> temp;
        a.inset(temp);
    }
}