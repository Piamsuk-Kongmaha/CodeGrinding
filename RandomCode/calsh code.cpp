#include <bits/stdc++.h>

using namespace std;

int main()
{
	string a;
	int ca,cb,cc,cd;

	cin >> a;
	int m = a.size(); 
	for (int i = 1; i <= m; i++)
	{
		if (a[i] == 'a')
		{
			ca++;
		}
		if (a[i] == 'b')
		{
			cb++;
		}
		if (a[i] == 'c')
		{
			cc++;
		}
		if (a[i] == 'd')
		{
			cd++;
		}
	}
	
	if
}
